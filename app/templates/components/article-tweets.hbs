<div class="panel panel-default">
  <table class="table">
    <tbody>
      {{#each tweets as |tweet|}}
        <tr>
          <td>
            <p>{{tweet.body}}</p>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
